<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- 상단: '설정' 타이틀 + 버튼 영역 -->
    <LinearLayout
        android:id="@+id/settings_main_panel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="24dp"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_settings_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="설정"
            android:textStyle="bold"
            android:textSize="28sp"
            android:textColor="?attr/colorOnSurface"
            android:paddingBottom="12dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_screen_settings"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="화면 설정"
            android:textAllCaps="false"
            app:cornerRadius="16dp"
            android:layout_marginTop="12dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_album_settings"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="앨범 설정"
            android:textAllCaps="false"
            app:cornerRadius="16dp"
            android:layout_marginTop="12dp"/>
    </LinearLayout>

    <!-- 세부 설정이 들어갈 컨테이너 (초기에는 숨김) -->
    <FrameLayout
        android:id="@+id/settings_child_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/settings_main_panel"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>